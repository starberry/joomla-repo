# This map consists of lines with a 'from' URI (without leading slash), a
# space, and a 'to' URI (with leading slash, or http://...) 
#
# It only affects URIs that have been fed into this map using RewriteRule.
#
# After modifying, run 'make'.

default.aspx /
index.php /
news.html /news
